# Development / CI dependencies. Installed on top of requirements.txt.
-r requirements.txt

ruff==0.15.19    # linter + formatter (pinned exactly so CI formatting is deterministic)
pytest~=9.1      # test runner for tests/test_app.py
pytest-cov~=7.1  # coverage reporting (pytest --cov in app-ci)
